import 'package:flutter/material.dart';
import 'package:flutter/painting.dart';

class AppColors {
  AppColors._();

  static const primaryColor = Color(0xFF055EB0);
  static const secondaryColor = Color(0xFFEBF2F9);
  static const light = Color(0xFFFFFFFF);
  static const dark = Color(0xFF454545);

  static const defaultShadow = BoxShadow(
    color: Color(0xFFEEEEEE),
    spreadRadius: 10,
    blurRadius: 10,
  );
}
